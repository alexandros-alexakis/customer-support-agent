# trash/

These files were moved here from the repository root when the `docs/` folder structure was introduced.

They are kept for reference and rollback purposes, not deleted. The canonical versions of this content now live under `docs/`.

Do not edit files in this folder. Edit the canonical versions under `docs/` instead.
